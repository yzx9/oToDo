kind: pipeline
type: exec
name: default

clone:
  disable: true

platform:
  os: linux
  arсh: amd64

steps:
- name: Run Commands
  commands:
    - su ss
    - cd /home/ss/src/oToDo
    - git pull
    - killall otodo
    - nohup go run ./